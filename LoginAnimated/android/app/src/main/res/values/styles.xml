<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/app_bg</item>
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="android:forceDarkAllowed">false</item>
        <!-- Android 12+ splash background color -->
        <item name="android:windowSplashScreenBackground">@color/app_bg</item>
    </style>

    <!-- Splash theme to show while Activity starts -->
    <style name="SplashTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="android:windowSplashScreenBackground">@color/app_bg</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/logo</item>
        <item name="android:windowSplashScreenAnimationDuration">400</item>
    </style>

    <!-- Theme used after content view is set -->
    <style name="AppTheme.NoActionBar" parent="AppTheme" />

</resources>
